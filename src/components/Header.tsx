import React from "react";

const Header = () => {
  return (
    <div
      style={{
        textAlign: "center",
        marginBottom: "16px",
        padding: "16px",
        fontSize: "40px",
        backgroundColor: "#1976d2",
        color: "white",
      }}
    >
      Tags popularity on stackoverflow
    </div>
  );
};

export default Header;
